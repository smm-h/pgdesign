{
  "checks": {
    "build": {
      "depends_on": [
        "validation"
      ],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "error",
      "tags": [
        "build"
      ]
    },
    "coverage": {
      "depends_on": [
        "validation"
      ],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "warn",
      "tags": [
        "coverage"
      ]
    },
    "design": {
      "depends_on": [
        "validation"
      ],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "warn",
      "tags": [
        "design"
      ]
    },
    "imports": {
      "depends_on": [
        "validation"
      ],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "error",
      "tags": [
        "imports"
      ]
    },
    "nf": {
      "depends_on": [],
      "fast": false,
      "needs_network": true,
      "pure": false,
      "severity": "warn",
      "tags": [
        "nf"
      ]
    },
    "revision": {
      "depends_on": [
        "validation"
      ],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "error",
      "tags": [
        "revision"
      ]
    },
    "structural": {
      "depends_on": [
        "validation"
      ],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "warn",
      "tags": [
        "design"
      ]
    },
    "validation": {
      "depends_on": [],
      "fast": true,
      "needs_network": false,
      "pure": true,
      "severity": "error",
      "tags": [
        "validation"
      ]
    },
    "workload": {
      "depends_on": [
        "validation"
      ],
      "fast": false,
      "needs_network": true,
      "pure": false,
      "severity": "warn",
      "tags": [
        "workload"
      ]
    }
  },
  "commands": {
    "build": {
      "effect": "mutating",
      "flags": [
        {
          "default": true,
          "help": "Automatically git commit the generated output files after a successful build; pass --no-auto-commit to leave them in the working tree",
          "name": "auto-commit",
          "negatable": true,
          "type": "bool"
        },
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL; required only when a [output.\u003cname\u003e.d2] sets live_stats=true",
          "name": "db",
          "type": "str"
        }
      ],
      "help": "Generate all configured outputs from pgdesign.toml",
      "name": "build"
    },
    "check": {
      "effect": "read_only",
      "flags": [
        {
          "default": false,
          "help": "Run every registered check regardless of tag or name filters",
          "name": "all",
          "negatable": true,
          "type": "bool"
        },
        {
          "default": "",
          "help": "Tag DSL expression to select checks (e.g. 'changelog \u0026 !quality')",
          "name": "tag",
          "type": "str"
        },
        {
          "default": "",
          "help": "Glob pattern to filter checks by name (e.g. 'hash-*', '*coverage*')",
          "name": "name",
          "type": "str"
        },
        {
          "default": false,
          "help": "List all registered checks with their tags and exit without running",
          "name": "list",
          "negatable": true,
          "type": "bool"
        },
        {
          "default": false,
          "help": "Treat warn-severity results as passing so they do not cause nonzero exit",
          "name": "ignore-warnings",
          "negatable": true,
          "type": "bool"
        }
      ],
      "forwarding": {
        "reason": "framework-internal: absorbs app-defined global flag values"
      },
      "help": "Run project checks registered via the check framework and report results",
      "name": "check",
      "payload_schema": {
        "items": {
          "type": "object"
        },
        "type": "array"
      }
    },
    "codegen": {
      "args": [
        {
          "help": "Path to TOML schema file(s) or directory containing them",
          "name": "path",
          "variadic": true
        }
      ],
      "effect": "mutating",
      "flags": [
        {
          "choices": [
            "python",
            "zig",
            "go",
            "ts",
            "java",
            "kotlin"
          ],
          "help": "Target programming language for the generated code",
          "name": "lang",
          "type": "str"
        },
        {
          "choices": [
            "constants",
            "constraints",
            "ddl",
            "drizzle",
            "enums",
            "gorm",
            "jpa",
            "query-layer",
            "sqlalchemy",
            "types",
            "validators"
          ],
          "default": "validators",
          "help": "Code generation mode determining what code to produce",
          "name": "mode",
          "type": "str"
        },
        {
          "help": "Write output to a file at this path instead of stdout",
          "name": "output",
          "type": "str"
        },
        {
          "choices": [
            "faceted",
            "self-contained"
          ],
          "help": "How to split multi-file Python DDL output: 'faceted' writes one file per object kind, 'self-contained' emits a single importable module",
          "name": "split-mode",
          "type": "str"
        },
        {
          "help": "Restrict generation to tables in these schema groups (matches build's per-output group filtering)",
          "name": "groups",
          "repeatable": true,
          "type": "list[str]",
          "unique": true
        },
        {
          "help": "Restrict generation to tables from these source file basenames (matches build's per-output source filtering)",
          "name": "source",
          "repeatable": true,
          "type": "list[str]",
          "unique": true
        },
        {
          "default": false,
          "help": "Verify generated code on disk is up to date without writing anything; requires --output, exits 1 on any missing, stale, or orphan file",
          "name": "check",
          "negatable": true,
          "type": "bool"
        }
      ],
      "help": "Generate type-safe application code from schema definitions",
      "name": "codegen"
    },
    "diff": {
      "args": [
        {
          "help": "Path to TOML schema file(s) or directory containing them",
          "name": "path",
          "variadic": true
        }
      ],
      "effect": "read_only",
      "flags": [
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL for live database comparison",
          "name": "live",
          "type": "str"
        },
        {
          "help": "Path to TOML schema file or directory to compare against",
          "name": "against",
          "type": "str"
        },
        {
          "help": "Git ref to compare the current schema against (e.g., main)",
          "name": "base",
          "type": "str"
        }
      ],
      "help": "Compare schema file(s) or directory against another target",
      "name": "diff",
      "payload_schema": {
        "additionalProperties": true,
        "properties": {
          "enums_added": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "enums_changed": {
            "items": {
              "type": "object"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "enums_removed": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "extensions_added": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "extensions_removed": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "tables_added": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "tables_changed": {
            "items": {
              "type": "object"
            },
            "type": [
              "array",
              "null"
            ]
          },
          "tables_removed": {
            "items": {
              "type": "string"
            },
            "type": [
              "array",
              "null"
            ]
          }
        },
        "required": [
          "tables_added",
          "tables_removed",
          "tables_changed",
          "enums_added",
          "enums_removed",
          "enums_changed",
          "extensions_added",
          "extensions_removed"
        ],
        "type": "object"
      }
    },
    "fmt": {
      "args": [
        {
          "help": "Path to the TOML schema file or directory to format",
          "name": "path"
        }
      ],
      "effect": "mutating",
      "flags": [
        {
          "default": false,
          "help": "Check if file is already formatted (exit 1 if not)",
          "name": "check",
          "negatable": true,
          "type": "bool"
        },
        {
          "choices": [
            "dependency",
            "alphabetical"
          ],
          "default": "dependency",
          "help": "Table ordering strategy: dependency-based or alphabetical",
          "name": "table-order",
          "type": "str"
        },
        {
          "choices": [
            "pk_fk_alpha",
            "alphabetical",
            "fk_last",
            "preserve"
          ],
          "default": "pk_fk_alpha",
          "help": "Column ordering: pk_fk_alpha, alphabetical, fk_last, or preserve",
          "name": "column-order",
          "type": "str"
        }
      ],
      "help": "Format a pgdesign TOML schema file or directory in place",
      "name": "fmt"
    },
    "generate": {
      "args": [
        {
          "help": "Path to TOML schema file(s) or directory containing them",
          "name": "path",
          "variadic": true
        }
      ],
      "effect": "read_only",
      "flags": [
        {
          "default": false,
          "help": "Add IF NOT EXISTS guards to all generated DDL statements",
          "name": "idempotent",
          "negatable": true,
          "type": "bool"
        },
        {
          "default": true,
          "help": "Include COMMENT ON statements in the generated output",
          "name": "comments",
          "negatable": true,
          "type": "bool"
        },
        {
          "choices": [
            "sql",
            "json",
            "d2",
            "svg",
            "doc",
            "graphql"
          ],
          "default": "sql",
          "help": "Output format for the generated schema representation",
          "name": "format",
          "type": "str"
        },
        {
          "default": false,
          "help": "Promote normal form violations to errors instead of warnings",
          "name": "strict-nf",
          "negatable": true,
          "type": "bool"
        }
      ],
      "help": "Generate SQL DDL from TOML schema file(s) or directory",
      "name": "generate"
    },
    "introspect": {
      "effect": "mutating",
      "flags": [
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL for the target database server",
          "name": "db",
          "type": "str"
        },
        {
          "default": [],
          "help": "PostgreSQL schema name(s) to introspect (repeatable)",
          "name": "schema",
          "repeatable": true,
          "type": "str",
          "unique": true
        },
        {
          "help": "Write output to a file at this path instead of stdout",
          "name": "output",
          "type": "str"
        },
        {
          "default": false,
          "help": "Discover extension types, functions, and opclasses",
          "name": "extensions",
          "negatable": true,
          "type": "bool"
        }
      ],
      "help": "Introspect a live PostgreSQL database into TOML schema",
      "name": "introspect"
    },
    "revise": {
      "effect": "mutating",
      "flags": [
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL for the non-retroactive DB tier (live FD discovery, pg_stat workload). When absent, the DB tier is skipped and revise exits non-zero after committing the pure tier.",
          "name": "db",
          "type": "str"
        },
        {
          "help": "Directory containing the chain-format migrations project (defaults to project config migrations_dir, else migrations)",
          "name": "dir",
          "type": "str"
        }
      ],
      "help": "Regenerate all outputs, chain the migration, and commit — the one-command project revision. Runs the PURE tier (build outputs, chain-mode migration, blocking normal-form and structural checks) and commits it, then runs the non-retroactive DB tier (live FD discovery, pg_stat workload).",
      "name": "revise"
    },
    "seed": {
      "args": [
        {
          "help": "Path to TOML schema file(s) or directory for seed generation",
          "name": "path",
          "variadic": true
        }
      ],
      "effect": "mutating",
      "flags": [
        {
          "default": 10,
          "help": "Number of rows to generate per table in the schema",
          "name": "rows",
          "type": "int"
        },
        {
          "help": "Random number generator seed for deterministic output",
          "name": "seed",
          "type": "int"
        },
        {
          "help": "Write output to a file at this path instead of stdout",
          "name": "output",
          "type": "str"
        },
        {
          "default": false,
          "help": "Insert generated seed data directly into the database",
          "name": "apply",
          "negatable": true,
          "type": "bool"
        },
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL, required when using --apply",
          "name": "db",
          "type": "str"
        },
        {
          "default": [],
          "help": "PostgreSQL schema name to filter seed generation to",
          "name": "schema",
          "repeatable": true,
          "type": "str",
          "unique": true
        },
        {
          "choices": [
            "insert",
            "copy"
          ],
          "default": "insert",
          "help": "SQL output format for generated seed data statements",
          "name": "format",
          "type": "str"
        },
        {
          "default": false,
          "help": "Emit TRUNCATE CASCADE statements before inserting seeds",
          "name": "clean",
          "negatable": true,
          "type": "bool"
        },
        {
          "choices": [
            "normal",
            "edge-cases"
          ],
          "default": "normal",
          "help": "Data generation strategy: normal values or edge-cases",
          "name": "mode",
          "type": "str"
        }
      ],
      "help": "Generate type-aware test data for all schema tables",
      "name": "seed"
    },
    "serve": {
      "effect": "read_only",
      "flags": [
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL for the target database server (omit for DB-free project mode)",
          "name": "db",
          "type": "str"
        },
        {
          "default": 8080,
          "help": "TCP port number for the HTTP API server to listen on",
          "name": "port",
          "type": "int"
        },
        {
          "default": "127.0.0.1",
          "help": "Network interface address to bind the HTTP server to. Defaults to 127.0.0.1 (loopback only). WARNING: the server has NO AUTHENTICATION; binding to a non-loopback address (e.g. 0.0.0.0) exposes the schema, database statistics, and diff endpoints to anyone who can reach that address.",
          "name": "bind",
          "type": "str"
        },
        {
          "default": [],
          "help": "PostgreSQL schema name to serve via the API (repeatable)",
          "name": "schema",
          "repeatable": true,
          "type": "str",
          "unique": true
        },
        {
          "default": 30,
          "help": "Maximum time in seconds for each HTTP request to complete",
          "name": "timeout",
          "type": "int"
        }
      ],
      "help": "Start the pgdesign HTTP API server and web interface",
      "name": "serve"
    },
    "stats": {
      "args": [
        {
          "help": "TOML schema file(s) for cross-referencing with live data",
          "name": "path",
          "required": false,
          "variadic": true
        }
      ],
      "effect": "read_only",
      "flags": [
        {
          "env": "PGDESIGN_DB",
          "help": "PostgreSQL connection URL for the target database server",
          "name": "db",
          "type": "str"
        },
        {
          "default": [],
          "help": "PostgreSQL schema name to analyze (repeatable for multiple)",
          "name": "schema",
          "repeatable": true,
          "type": "str",
          "unique": true
        }
      ],
      "help": "Analyze database statistics, index usage, and health",
      "name": "stats",
      "payload_schema": {
        "additionalProperties": false,
        "properties": {
          "cache_hit_ratio": {
            "type": "number"
          },
          "database": {
            "type": "string"
          },
          "duplicate_indexes": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "index": {
                  "type": "string"
                },
                "schema": {
                  "type": "string"
                },
                "superset_index": {
                  "type": "string"
                },
                "table": {
                  "type": "string"
                }
              },
              "required": [
                "schema",
                "table",
                "index",
                "superset_index"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "tables": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "dead_tuples": {
                  "type": "integer"
                },
                "last_vacuum": {
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "live_tuples": {
                  "type": "integer"
                },
                "name": {
                  "type": "string"
                },
                "schema": {
                  "type": "string"
                },
                "seq_scans": {
                  "type": "integer"
                },
                "vacuum_needed": {
                  "type": "boolean"
                }
              },
              "required": [
                "schema",
                "name",
                "live_tuples",
                "dead_tuples",
                "seq_scans",
                "last_vacuum",
                "vacuum_needed"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "unused_indexes": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "index": {
                  "type": "string"
                },
                "scans": {
                  "type": "integer"
                },
                "schema": {
                  "type": "string"
                },
                "table": {
                  "type": "string"
                }
              },
              "required": [
                "schema",
                "table",
                "index",
                "scans"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "vacuum_candidates": {
            "items": {
              "additionalProperties": false,
              "properties": {
                "dead_ratio": {
                  "type": "number"
                },
                "dead_tuples": {
                  "type": "integer"
                },
                "live_tuples": {
                  "type": "integer"
                },
                "schema": {
                  "type": "string"
                },
                "table": {
                  "type": "string"
                }
              },
              "required": [
                "schema",
                "table",
                "live_tuples",
                "dead_tuples",
                "dead_ratio"
              ],
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "database",
          "cache_hit_ratio",
          "tables",
          "unused_indexes",
          "vacuum_candidates",
          "duplicate_indexes"
        ],
        "type": "object"
      }
    }
  },
  "defaults": {
    "app": {
      "commands": {},
      "config": false,
      "deprecated": {},
      "env_prefix": null,
      "global_flags": [],
      "groups": {},
      "tag_contracts": {}
    },
    "arg": {
      "choices": null,
      "default": null,
      "required": true,
      "type": "str",
      "variadic": false
    },
    "command": {
      "args": [],
      "constraints": [],
      "flags": [],
      "hidden": false,
      "interactive": false,
      "passthrough": false,
      "tags": []
    },
    "flag": {
      "choices": null,
      "default": null,
      "env": null,
      "env_separator": null,
      "hidden": false,
      "negatable": null,
      "repeatable": false,
      "short": null,
      "unique": false
    },
    "group": {
      "commands": {},
      "deprecated": {},
      "groups": {},
      "hidden": false,
      "tags": []
    },
    "schema_version": 1
  },
  "global_flags": [
    {
      "help": "Path to pgdesign.toml (bypasses directory search)",
      "name": "project-config",
      "type": "str"
    }
  ],
  "groups": {
    "import": {
      "commands": {
        "lock": {
          "args": [
            {
              "help": "Name of the single [imports] alias to lock; omit to lock every alias declared in pgdesign.toml",
              "name": "alias",
              "required": false
            }
          ],
          "effect": "mutating",
          "help": "Resolve each [imports] alias's git pin, vendor the referenced surface (tables + type closure) into imports/\u003calias\u003e/, and write the lockfile. Refuses to overwrite an existing lockfile — use `import update` to re-pin.",
          "name": "lock"
        },
        "update": {
          "args": [
            {
              "help": "Name of the single [imports] alias to re-resolve and update; omit to update every declared alias",
              "name": "alias",
              "required": false
            }
          ],
          "effect": "mutating",
          "help": "Re-resolve each [imports] alias's git ref, re-extract the surface your schema actually references, and rewrite the lockfile at the new commit. Each declared framework is cloned into a temp directory that is removed on return, and only the tables your schema uses are vendored. Requires an existing lockfile — use `import lock` for the first pin. Pass an alias to update just that one.",
          "name": "update"
        }
      },
      "help": "Pin and vendor imported schema surfaces from other pgdesign projects",
      "name": "import"
    },
    "migrate": {
      "commands": {
        "apply": {
          "consequential": true,
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            }
          ],
          "help": "Apply all pending migrations to the target database in order. Each migration runs inside its own transaction with advisory locking to prevent concurrent execution. Non-transactional operations like CREATE INDEX CONCURRENTLY execute outside transactions automatically. Use --dry-run to preview the SQL without executing.",
          "name": "apply"
        },
        "baseline": {
          "args": [
            {
              "help": "Schema TOML file(s) or directory (chain mode; selects the schema search-path to introspect)",
              "name": "path",
              "required": false,
              "variadic": true
            }
          ],
          "consequential": true,
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            },
            {
              "default": "",
              "help": "Version label for the baseline record (legacy semver-TOML mode only)",
              "name": "version",
              "type": "str"
            },
            {
              "default": "Initial baseline",
              "help": "Human-readable note recorded with the baseline, shown in migrate status and history output",
              "name": "description",
              "type": "str"
            }
          ],
          "help": "Adopt an existing database onto the migration chain without executing any migration SQL — for a schema created by other means, or one that has intentionally drifted from the TOML. In chain mode it introspects the live database, synthesizes a genesis edge carrying that manifest, and stamps the baseline boundary (rollback-frozen); pass the schema TOML path(s) so the right search-path is introspected. Legacy semver-TOML mode records a semver --version instead. Re-baselining at the same state is a no-op.",
          "name": "baseline"
        },
        "generate": {
          "args": [
            {
              "help": "Path to TOML schema file(s) or directory containing them",
              "name": "path",
              "variadic": true
            }
          ],
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server (legacy-mode only; chain-mode generate is pure)",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            }
          ],
          "help": "Generate versioned migration files by comparing the TOML schema against a live database. Produces up and down SQL files with risk annotations, safety linting, and expand-migrate-contract phase classification. Volatile defaults and operations on large tables are automatically detected and handled safely.",
          "name": "generate"
        },
        "plan": {
          "args": [
            {
              "help": "Path to TOML schema file(s) or directory containing them",
              "name": "path",
              "variadic": true
            }
          ],
          "effect": "read_only",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL (legacy-mode only; chain-mode plan is pure and ignores it)",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Chain-mode only: revision string to enumerate from (pure input); absent enumerates from genesis",
              "name": "from",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            }
          ],
          "help": "Preview the migration chain PURELY, without touching any database (roadmap 5.9). In a chain-mode project it enumerates the edges from GENESIS -- or from an explicit --from revision -- to the single live head, in path-finder order, listing each edge's id, slug, and op summary. Drift preview against a live database is `diff --live`'s job; per-database pending is `migrate status`'s. A legacy (semver-TOML) project keeps the old live-diff plan (requires --db) until it is upgraded to the chain.",
          "name": "plan"
        },
        "rebase": {
          "effect": "mutating",
          "flags": [
            {
              "help": "The head to KEEP (a revision string or a live edge-id prefix); the OTHER head's tail is re-parented onto it",
              "name": "head",
              "type": "str"
            },
            {
              "help": "Directory containing the chain project (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            }
          ],
          "help": "Resolve a two-head fork (chain mode). Re-parents the tail of the head NOT named by --head onto the head that IS named, re-simulating each re-parented edge's ops to recompute its revision and content-derived edge file. The rebased-away originals retire INTACT to migrations/archive/ (never rewritten or deleted), and the rebase revision-remap table (migrations/remap.json) is written so a database stamped at a rebased-away revision is served forward to the live head, never orphaned. A pure file operation: no database is required.",
          "name": "rebase"
        },
        "rollback": {
          "consequential": true,
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            },
            {
              "default": "",
              "help": "Target version to rollback to (exclusive -- this version stays applied)",
              "name": "to",
              "type": "str"
            }
          ],
          "help": "Rollback applied database migrations to a specified target version. Executes down migration SQL in reverse application order with advisory locking. Multi-step rollbacks verify reversibility of all steps before starting. The target version is exclusive, meaning that version stays applied after rollback completes.",
          "name": "rollback"
        },
        "squash": {
          "effect": "mutating",
          "flags": [
            {
              "help": "Start of the squash range: a semver version (legacy) or a revision-or-edge reference (chain mode; 'genesis', a revision string, or a live edge-id prefix)",
              "name": "from",
              "type": "str"
            },
            {
              "help": "End of the squash range: a semver version (legacy) or a revision-or-edge reference (chain mode)",
              "name": "to",
              "type": "str"
            },
            {
              "help": "Display slug for the consolidation edge (chain mode; auto-derived from endpoint hashes when omitted)",
              "name": "slug",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            },
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL (REQUIRED); the pre-upgrade guard runs against it (legacy mode also runs the M200 applied-version check)",
              "name": "db",
              "type": "str"
            }
          ],
          "help": "Consolidate a range of sequential migrations. In chain mode (a migrations/chain/ project) squash mints a CONSOLIDATION EDGE whose op-list is the ordered concatenation of the range, retiring the superseded originals intact to migrations/archive/ (never a rewrite) so mid-range databases resume via the path-finder; --from/--to are revision-or-edge references. In legacy (semver-TOML) mode squash concatenates the range into one combined migration file. --db is required.",
          "name": "squash"
        },
        "status": {
          "effect": "read_only",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            }
          ],
          "help": "Show which migrations have been applied to the target database and which are still pending. Reads the migration tracking table and compares it with the migrations directory to display version numbers, applied timestamps, and current execution status for each migration file.",
          "name": "status"
        },
        "test": {
          "args": [
            {
              "help": "Schema file(s) or directory (required with --shadow)",
              "name": "path",
              "required": false,
              "variadic": true
            }
          ],
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the staging test database",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            },
            {
              "default": 60,
              "help": "Maximum time in seconds before the test run is aborted",
              "name": "timeout",
              "type": "int"
            },
            {
              "default": false,
              "help": "Test by replaying migrations into a shadow database and diffing against TOML schema",
              "name": "shadow",
              "negatable": true,
              "type": "bool"
            }
          ],
          "help": "Test migrations by applying them against a staging database to verify correctness before production deployment. With --shadow mode, replays all migrations into a fresh database and diffs the result against the TOML schema to catch drift between migration files and schema definitions.",
          "name": "test"
        },
        "upgrade": {
          "args": [
            {
              "help": "Path to TOML schema file(s) or directory containing them",
              "name": "path",
              "variadic": true
            }
          ],
          "consequential": true,
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the database to upgrade",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Directory containing migration files to read or write (defaults to project config migrations_dir, else migrations)",
              "name": "dir",
              "type": "str"
            }
          ],
          "help": "One-time adoption of a legacy (semver-TOML) database onto the on-disk chain. Verifies the schema TOML matches the live database exactly (refusing to stamp over drift), folds the existing pgdesign_migrations rows into the chain journal, writes the content-addressed prefix edge, and stamps this database's upgrade boundary in a single transaction. Requires a clean working tree for the schema files when inside a git repository. Run once per database; a fresh database uses `migrate apply` directly.",
          "name": "upgrade"
        }
      },
      "help": "Database migration planning, generation, and execution",
      "name": "migrate"
    },
    "testdb": {
      "commands": {
        "gc": {
          "consequential": true,
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Drop databases older than this duration (e.g., 2h, 30m)",
              "name": "older-than",
              "type": "str"
            }
          ],
          "help": "Drop orphaned test databases that were not properly torn down after test runs. Scans the PostgreSQL server for databases matching the pgdesign test naming pattern and removes those older than the specified duration. Useful for cleaning up after interrupted or failed test runs in CI and local development.",
          "name": "gc"
        },
        "init": {
          "effect": "mutating",
          "flags": [
            {
              "default": [],
              "help": "Target programming language(s) for wrapper generation",
              "name": "language",
              "repeatable": true,
              "type": "str",
              "unique": true
            },
            {
              "help": "Name of the SQL output section (for disambiguation)",
              "name": "output",
              "type": "str"
            },
            {
              "default": false,
              "help": "Overwrite existing wrapper files without prompting",
              "name": "force-overwrite",
              "negatable": true,
              "type": "bool"
            },
            {
              "help": "CI provider for workflow generation (e.g., github-actions)",
              "name": "ci",
              "type": "str"
            },
            {
              "default": false,
              "help": "Include pg_partman installation step in CI workflow",
              "name": "partman",
              "negatable": true,
              "type": "bool"
            }
          ],
          "help": "Generate test database wrapper code for consumer projects that need to run integration tests against a pgdesign-managed schema. Produces language-specific helper modules with setup and teardown functions that create ephemeral databases, apply DDL, and clean up automatically after each test run.",
          "name": "init"
        },
        "setup": {
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            },
            {
              "help": "Path to the SQL DDL file to apply to the test database",
              "name": "ddl",
              "type": "str"
            }
          ],
          "help": "Create an ephemeral test database on the PostgreSQL server and apply the specified DDL schema to it. The database is created with a unique name containing a timestamp and random suffix to allow parallel test execution. Returns the connection URL for the new database.",
          "name": "setup"
        },
        "teardown": {
          "consequential": true,
          "effect": "mutating",
          "flags": [
            {
              "env": "PGDESIGN_DB",
              "help": "PostgreSQL connection URL for the target database server",
              "name": "db",
              "type": "str"
            }
          ],
          "help": "Drop an ephemeral test database that was previously created by testdb setup. Terminates any remaining connections to the database before dropping it. Should be called in test cleanup to prevent orphaned databases from accumulating on the PostgreSQL server over time.",
          "name": "teardown"
        }
      },
      "help": "Manage ephemeral test databases for schema testing",
      "name": "testdb"
    }
  },
  "help": "PostgreSQL schema compiler",
  "infra": {
    "connections": [
      {
        "env_var": "PGDESIGN_DB",
        "help": "PostgreSQL connection URL for database-backed commands and checks"
      }
    ]
  },
  "name": "pgdesign",
  "project_id": "github.com/smm-h/pgdesign",
  "schema_version": 1,
  "version": ""
}

# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Fix CWD-dependent panic and add view/materialized view support"
# Optional context explaining why these changes were made
context = "pgdesign panicked when run from any directory that did not contain .strictcli/checks.toml. The fix embeds checks.toml at compile time via go:embed. Additionally, validation, risk assessment, and migration support have been extended to views and materialized views."
# Set to true to generate a blog post for this release
# blog = false
include = ["go", "npm", "pypi"]
exclude = []

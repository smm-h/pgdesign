# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "patch"
# Short description of this release (required)
description = "Bug fixes for JDBC testdb URL handling and migrate generate column serialization."
# Optional context explaining why these changes were made
context = "Java and Kotlin testdb consumers should re-run `pgdesign testdb init` to pick up the JDBC URL fix. No codegen template changes in this release."
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["go", "npm", "pypi"]
exclude = []

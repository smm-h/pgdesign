# Version bump type: patch, minor, or major
bump = "patch"
# Short description of this release (required)
description = "Critical panic fix, UTF-8-safe truncation, DDL error diagnostics, selfdoc coverage"
# Optional context explaining why these changes were made
context = "Fixes a critical bug where the pgdesign binary panicked on every invocation since v0.14.0 due to a missing strictcli flag configuration. Also fixes UTF-8 truncation in wrapper templates, adds SQL previews to DDL error messages, and adds selfdoc documentation for 6 previously undocumented packages."
# Set to true to generate a blog post for this release
# blog = false
include = ["go", "npm", "pypi"]
exclude = []

# Version bump type: patch, minor, major, hotfix, or prerelease
bump = "minor"
# Short description of this release (required)
description = "Partition lifecycle completeness, migrate baseline fix, library promotion (pkg/diff, pkg/genkit, pkg/diagnostic)."
# Optional context explaining why these changes were made
context = """
Partman integration reaches end-to-end coverage: CI template gains --partman flag \
with postgres+partman service, introspection excludes partman-managed children from \
drift, MaintenanceDiff safely updates part_config with hard error on interval changes, \
and extension setup is schema-qualified. Partition interval is now separated from \
retention and required explicitly (breaking). Migrate baseline records all versions \
with additive idempotency and out-of-order guard. Three internal packages promoted to \
shared libraries: pkg/diff, pkg/genkit, pkg/diagnostic."""
# Pre-release identifier: alpha, beta, rc, or stable
# preid = ""
# Set to true to generate a blog post for this release
# blog = false
include = ["go", "npm", "pypi"]
exclude = []

# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Schema object expansion, codegen modes, BCNF audit, functions, triggers, domains"
# Optional context explaining why these changes were made
context = """
Major feature release adding six new schema object types (domains, exclusion constraints, \
standalone sequences, composite types, functions/procedures, triggers) with full pipeline \
support (parse, model, DDL, diff, migrate, introspect). Codegen expanded from 3 modes to 9 \
(types, constraints, gorm, drizzle, sqlalchemy, jpa added). BCNF audit with decomposition \
algorithms, Armstrong relation counterexamples, and FD inference from PK/UNIQUE constraints. \
Scalar types now produce CREATE DOMAIN for database-level enforcement. Generic TopoSort and \
matchObjects infrastructure reduces code duplication. Multi-column partition keys, per-column \
collation and statistics, range/multirange types, identity column introspection, and \
deferrable constraint support round out the release."""
# Set to true to generate a blog post for this release
# blog = false
include = ["go", "npm", "pypi"]
exclude = []

# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "Pool configuration, OR-compound and NOT EXISTS codegen support, multi-table and multi-flag fixes."
# Optional context explaining why these changes were made
context = ""
include = ["go", "npm", "pypi"]
exclude = []

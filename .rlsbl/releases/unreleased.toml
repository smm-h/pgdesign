# Version bump type: patch, minor, or major
bump = "minor"
# Short description of this release (required)
description = "State machine types, Python query-layer codegen, table groups, and constraint codegen for 3 new languages"
# Optional context explaining why these changes were made
context = """
This release introduces state machine types (kind = "state_machine") as a first-class schema concept \
with PG enum creation, PL/pgSQL enforcement triggers, D2 state diagrams, diff/migration support, and \
codegen transition maps for all 6 languages. Python codegen gains two new modes: DDL-as-data (pure tuples \
with a section executor) and query-layer (protocol-based PgBackend and InMemoryBackend with declarative \
constraint registry). Constraint codegen expands from 2 to 5 languages (adding Python, Java, Kotlin). \
Table groups enable config-based output filtering. Breaking: unknown type kind values now error instead \
of silently defaulting to scalar."""
# Set to true to generate a blog post for this release
# blog = false
include = ["go", "npm", "pypi"]
exclude = []
